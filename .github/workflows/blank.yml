name: CI

on: [push]

jobs:
  testStuffOut:
    runs-on: windows-latest
    steps:
    - name: see if msbuild is installed
      run: |
        git clone https://github.com/ntop/n2n.git
        cd n2n
        md build
        cd build
        cmake ..
