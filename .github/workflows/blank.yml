name: CI

on: [push]

jobs:
  testStuffOut:
    runs-on: windows-latest
    steps:
    - name: see if msbuild is installed
      run: |
        git clone https://github.com/ntop/n2n.git
        cd n2n
        md build
        cd build
        env
        netstat
        bash.exe
        # set "OPENSSL_CRYPTO_LIBRARY=C:\Program Files\Git\usr\bin\openssl.exe"
        # cmake -DOPENSSL_CRYPTO_LIBRARY=%OPENSSL_ROOT_DIR%\lib ..
